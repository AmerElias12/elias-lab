Collaborator logos
==================

Drop each logo here using these EXACT file names
(PNG with a transparent background works best; ~200px tall):

  braude-college.png          <-- host institution, shown in the footer
                                  (Braude College of Engineering, Karmiel)
  tel-aviv-university.png
  university-of-haifa.png
  emendo-bio.png
  rabin-medical-center.png
  galilee-medical-center.png

Until a file is added, that slot shows a clean text badge (BRAUDE, TAU, UoH,
EB, RMC, GMC) instead of a broken image — so the page never looks broken.

If you have a logo in another format (.svg, .jpg), either rename it to the
.png name above (browsers still load it) or tell me and I'll update the code
to point at the exact filename/format.

Note: use logos you have permission to display (official partner logos).
